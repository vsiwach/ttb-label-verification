# Heavy ML deps for INFERENCE_MODE=sft.
# Kept out of requirements.txt so mock/cloud/claude-code/onprem users don't
# pay the ~3 GB torch install cost.
#
# Install with:   pip install -r backend/requirements-sft.txt
#
# CUDA users: install torch from https://pytorch.org/ FIRST with the right
# CUDA wheel, then run this file. On CPU/Mac the line below is fine.
#
# Inference is BF16 throughout — no quantization, so no bitsandbytes here.
# (bitsandbytes is only needed at training time for the adamw_8bit optimizer;
# that pin lives in the training notebooks' install cells.)

torch>=2.5
transformers==4.51.3
peft>=0.13
torchao>=0.16
accelerate>=0.30
safetensors
sentencepiece
protobuf>=5.27
Pillow
