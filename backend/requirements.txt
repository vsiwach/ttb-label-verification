# Pins bumped to clear the 14-CVE backlog before public deployment.
# Comments tag which CVEs each line closes; see pip-audit output for IDs.
fastapi==0.135.4              # last clean release before 0.136.3 supply-chain compromise (MAL-2026-4750);
                              # still pulls in starlette >=0.47 which closes 2 of 3 starlette CVEs
uvicorn[standard]==0.48.0
python-multipart==0.0.29      # closes 3 multipart CVEs (CVE-2026-24486/40347/42561)
pydantic==2.13.4
pydantic-settings==2.7.0
pytest==9.0.3                 # closes CVE-2025-71176 (dev-only impact)
pytest-asyncio==1.4.0         # needed for pytest 9.x compatibility
httpx==0.28.1
requests==2.34.2              # closes CVE-2024-47081, CVE-2026-25645
Pillow==12.2.0                # closes 5 CVEs (CVE-2026-25990/40192/42308/42310/42311)
anthropic>=0.104.0
