# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.swp

# Local env
.env
.env.local
.env.*.local

# SFT model bundles — large weights, kept out of git. Unzip from ~/Downloads to use.
backend/models/
test/eval/__pycache__/

# UAT pack — regenerated by `make uat-pack` (67 webp images, redundant
# with the COLA Cloud sample).
test/eval/uat_pack/

# Local Claude Code config (per-user, not part of the deliverable).
.claude/

# Qwen v2 training pipeline — regenerated by build_qwen_jsonl.py / scrape.log
test/eval/data/qwen_sft/
test/eval/data/ttb_live/scrape.log
.pipeline_state/
