# Downloaded source data — regenerated by `make build-eval-data`.
cola-sample-pack-v1.zip
cola.csv
cola_image.csv
cola_image_barcode.csv
README.txt

# Downloaded images (regenerated by build_dataset.py).
images/*.webp

# Keep cola_sample.csv — small, reproducible, references downloaded images.

# TTB-live scrape (test/eval/scrape_ttb_registry.py) — keep the CSV (the
# structured data extracted from Form 5100.31), drop the bulk image
# files (425 composite JPEGs + per-COLA debug panels — ~500 MB,
# regenerated by `make scrape-ttb`).
ttb_live/images/
