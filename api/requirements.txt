# Vercel @vercel/python installs requirements from THIS file (next to
# api/index.py), NOT from backend/requirements.txt. Keep these pins in
# sync with backend/requirements.txt so the Vercel deploy uses the same
# CVE-patched versions as the Modal deploy + the Mac dev backend.

fastapi==0.135.4
python-multipart==0.0.29
pydantic==2.13.4
pydantic-settings==2.7.0
httpx==0.28.1
requests==2.34.2
Pillow==12.2.0
anthropic>=0.104.0

# uvicorn / starlette: Vercel's @vercel/python runtime provides its own
# ASGI server, so we don't pin uvicorn here. starlette comes in as a
# fastapi transitive — pinned implicitly to >=1.1 via fastapi 0.135.4.
